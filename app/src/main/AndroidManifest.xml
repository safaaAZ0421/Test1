<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test1"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.test1"
        tools:targetApi="31">

        <activity android:name=".SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".activities.ServiceLectureActivity" />
        <activity android:name=".activities.BibliothequeDetailActivity"/>
        <activity android:name=".activities.ForfaitsActivity"/>
        <activity android:name=".activities.FormulaireAbonnementActivity"/>
        <activity android:name=".activities.AdherentActivity"/>
        <activity android:name=".activities.RechercheLivresActivity" />




        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.test1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
